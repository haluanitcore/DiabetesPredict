# Python dependencies for the diabetes prediction model (inference only).
#
# Versi di bawah HARUS sama dengan `versi_library` pada model/model_metadata.json,
# yaitu versi yang dipakai ketika rf_model.pkl & scaler.pkl dibuat. Memuat pickle
# dengan versi scikit-learn berbeda memicu InconsistentVersionWarning dan, pada
# perbedaan versi yang cukup jauh, dapat mengubah hasil prediksi tanpa error.
#
# Model aktif : v3.0-revisi
# Dihasilkan  : Revisi_Pengujian_V3/DiaPredict_Revisi_V3_GABUNGAN.ipynb (BAGIAN 7)
# Dibuat pakai: scikit-learn 1.9.0, numpy 2.5.1, Python 3.12
#
# Bila model dilatih ulang, perbarui berkas ini agar cocok dengan versi_library
# pada model_metadata.json yang baru.

scikit-learn==1.9.0
numpy==2.5.1
scipy==1.18.0

# Hanya dibutuhkan saat MELATIH ulang model lewat notebook Revisi_Pengujian_V3/,
# bukan saat inference:
# pandas==3.0.3
# imbalanced-learn==0.14.2
# kagglehub==1.0.2
