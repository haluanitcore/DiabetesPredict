# Python dependencies for the diabetes prediction model (inference only).
#
# ✅ TERVERIFIKASI: model rf_model.pkl & scaler.pkl di-pickle dengan scikit-learn
# 1.8.0 (dibaca langsung dari file model). Versi di bawah = versi PERSIS yang
# terbukti memuat model tanpa warning versi & menghasilkan prediksi valid.
# JANGAN turunkan versi scikit-learn — versi lebih rendah (mis. 1.5.2) memicu
# InconsistentVersionWarning dan hasil prediksi bisa keliru.

scikit-learn==1.8.0
numpy==2.5.1
scipy==1.18.0

# Hanya dibutuhkan saat MELATIH ulang model (train_model.py), bukan saat inference:
# pandas==2.2.3
# imbalanced-learn==0.12.4
# kagglehub==0.3.4
